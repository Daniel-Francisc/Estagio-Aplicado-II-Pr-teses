import React from 'react';

function Homepage() {
    return (
        <div>

        </div>
    );
}

const styles = {
    container: {
        textAlign: 'center',
    },
};

export default Homepage;
