import React from 'react';

function Login() {
    return (
        <div style={{
            display: "flex",
            backgroundColor: "red"
        }}>
            <h1>Login</h1>
        </div>
    );
}

const styles = {
    container: {
        textAlign: 'center',
    },
};

export default Login;
