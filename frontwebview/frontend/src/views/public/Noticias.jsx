import React from 'react';
import Navbar from '../../components/navbar';

function Noticias() {
    return (
        <main>
            <Navbar/>

        </main>
    );
}

const styles = {
    container: {
        textAlign: 'center',
    },
};

export default Noticias;