import React from 'react';
import Navbar from '../../components/navbar';

function Homepage() {
    return (
        <main>
            <Navbar />
            <text>testando</text>
        </main>
    );
}

const styles = {
    container: {
        textAlign: 'center',
    },
};

export default Homepage;
