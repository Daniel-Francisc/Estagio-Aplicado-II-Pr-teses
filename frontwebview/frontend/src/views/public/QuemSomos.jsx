import React from 'react';
import Navbar from '../../components/navbar';

function QuemSomos() {
    return (
        <main>
            <Navbar/>
        </main>
    );
}

const styles = {
    container: {
        textAlign: 'center',
    },
};

export default QuemSomos;